import { createApp } from 'vue'

import App from './App.vue'

// import {hunhe,hunhe2} from './mixin'

createApp(App).mount('#app')
// createApp(App).config.productionTip=false
// createApp(App).mixin(hunhe)
// createApp(App).mixin(hunhe2)
